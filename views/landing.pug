extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action ="/" , method = "POST")
    input(type="email" , name ="email_lead", placeholder ="Enter your email address")
    button(type="submit") Submit
